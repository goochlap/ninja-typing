<h1>Ninja Typing -- Home Page</h1>
<br>
<% if current_user && !current_user.avatar.nil? %>
  <%= link_to 'Avatar show', user_avatar_path(current_user, current_user.avatar) %>
<% end %>
<br>
<% if current_user && current_user.avatar.nil? %>
  <%= link_to 'Create Your Avatar', new_user_avatar_path(current_user) %>
<% end %>

<%#= link_to 'PlAY!', user_board(current_user, current_user.avatar.board)  %>
